import {Apadrinamiento } from "../../components";
import { SponsorChildViewProps } from './types';

export const SponsorChild: React.FC = (props: SponsorChildViewProps) => (<Apadrinamiento {...props}/>);