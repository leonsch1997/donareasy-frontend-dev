import { RecoverSteps } from "../../components";
import { RecoverPasswordViewProps } from './types';

export const Recover: React.FC = (props: RecoverPasswordViewProps) => (<RecoverSteps {...props}/>);