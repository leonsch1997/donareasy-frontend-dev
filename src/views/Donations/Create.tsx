import { CrearDonacion as CreateMain } from "../../components";
import { CreateDonationViewProps } from './types';

export const CreateDonation: React.FC = (props: CreateDonationViewProps) => (<CreateMain {...props}/>);